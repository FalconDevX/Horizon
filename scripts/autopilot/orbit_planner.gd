class_name OrbitPlanner
extends RefCounted

static func plan_hohmann_transfer(frame: OrbitalFrame, current_state: OrbitState, target_radius: float) -> Array:
    # Tylko dla jednej planety (kołowanie)
    var maneuvers = []
    # (To be implemented fully later)
    return maneuvers
